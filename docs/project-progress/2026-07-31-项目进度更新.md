# 2026-07-31 竞品洞察 Agent 证据型报告里程碑

## 结论

竞品洞察 Agent 已在本地完成抖音账号证据型报告闭环：既可从抖音账号抓取结果的精确 Excel 路径自动分析，也可直接上传已有 `.xlsx`；数据层确定性生成证据包，模型按三批返回结构化判断，终检通过后保存、预览和下载同一份 Markdown。

本里程碑属于“功能与脱敏数据验收完成”，不是“真实业务验收完成”。本次没有真实抓取、没有读取 Cookie 或登录态、没有调用真实模型、没有使用 API Key，也没有部署或推送。报告桥已迁移到空闲固定端口 `127.0.0.1:8768`，未触碰占用 `8767` 的无关 `NewMediaCardMaker/0.4`。

## 已完成

- 保留抖音和小红书独立采集 Skill、独立桥和平台自动路由，小红书不会误用抖音字段或报告模板。
- 抖音账号抓取成功响应增加精确绝对 `excelPath`；抖音单作品仍只抓取作品，不触发账号报告。
- 新增“抓取并分析”和“分析已有 Excel”双入口。
- 完成 Excel 工作表与字段语义识别、互动数字清洗、缺失字段记录和稳定排序。
- 固定综合互动量为点赞、评论、收藏、分享之和。
- 完成 Top 10、起号期 Top 5、高收藏、高分享、高评论和汇总指标的确定性计算。
- 生成稳定 `DY-E` 证据编号、数据完整性摘要、确定性三批 `batchInputs` 和本地证据 JSON。
- 按 `strategy / performance / execution` 三批调用竞品 Agent 当前模型；官方模型走同源服务端代理，只有已审核的 APINebula 精确域名允许浏览器直连。
- 完成模型结构 Schema、批次归属、证据 ID、数字、固定章节、医药健康合规和最终全文一致性校验。
- 支持停止、凭据修订失效、迟到响应丢弃和失败批次重试；重试不重复上传或抓取。
- Markdown 预览和下载使用同一次组装结果，报告使用独占文件名保存，不覆盖历史文件。
- 原始 Excel 不改写；上传临时副本在证据生成后删除。
- 报告、证据、上传快照和 `.tmp` 逐级拒绝符号链接/非目录，使用目录 fd 相对独占或原子写入。
- 工作簿必须包含独立抖音账号信息表与作品表；strategy 批次获取限长昵称、粉丝、签名上下文。
- 模型格式或章节证据校验失败时，仅当前批次自动安全重试一次；鉴权、限流、超时、取消和传输错误不自动重试。
- 外部抖音 Skill 位于 `/Users/lvyakun/.codex/skills/douyin-scraper`，离线契约测试 6 / 6 通过。

## 进行中

- 抖音真实竞品账号首次业务验收：需由用户在本机完成平台验证，核对真实导出字段、报告结论质量和可执行价值。
- 报告桥已迁移到 `127.0.0.1:8768`；真实账号业务验收前仍需确认本机常驻启动方式。
- 小红书真实目标首次采集验收：Skill、`127.0.0.1:8766` 和前端路由已接入，但未启动真实登录或抓取。

## 尚未完成

- 小红书字段、图文/笔记形态和互动口径专属的竞品分析报告。
- 聊天、任务、证据包和报告成果的数据库持久化。
- 正式线上站点调用用户本机的抖音、小红书和报告桥。
- 真实账号、真实模型和实际经营场景下的业务准确性、成本和耗时验收。
- 自动部署、多人协作、权限审计和长期任务队列。

## 验收结果

| 验证项 | 结果 |
|---|---|
| Python 竞品运行时全量 | 88 / 88 通过 |
| `npm test` | 构建通过，Node / React 330 / 330 通过 |
| TypeScript 类型检查 | 通过 |
| Lint | 通过，0 error / 0 warning |
| 外部抖音 Skill 离线契约 | 6 / 6 通过 |
| 脱敏 Excel 双入口 | 上传与受控路径分析均返回真实证据包和三批真实 `batchInputs` |
| 三批 mock 模型结果 | `strategy / performance / execution` 全部通过真实章节校验 |
| Markdown 组装 | 真实落盘并回读一致 |
| 固定报告内容 | Top 10、起号期 Top 5、5 个选题方向、3 个拍法模板、7 天清单齐全 |
| 原始夹具完整性 | 分析前后 SHA-256 一致 |
| 抖音采集桥 `8765` | `DouyinScraperBridge/1.0`，健康 200，恶意 Origin 写请求 403 |
| 小红书采集桥 `8766` | `XiaohongshuScraperBridge/1.0`，健康 200，恶意 Origin 写请求 403 |
| 固定报告桥 `8768` | 健康响应标识 `competitor-insight-report` 且返回 200，恶意 Origin 写请求 403 |
| 无关服务 `8767` | `NewMediaCardMaker/0.4` 保持原状，未终止、重启或改动 |
| 真实平台与真实模型 | 均未执行 |
| 发布状态 | 本地分支，未部署、未推送 |

## 输出位置

- 抖音抓取 Excel：`outputs/competitor-insight/douyin/`
- 证据包：`outputs/competitor-insight/reports/evidence/`
- Markdown 报告：`outputs/competitor-insight/reports/`

同名报告增加时间戳和冲突序号，不覆盖历史结果。端到端验收使用临时目录，未把脱敏 Excel、证据 JSON 或 Markdown 产物加入 Git。

## 隐私、安全与经营边界

- 本次未读取、导出或记录 Cookie、浏览器登录态、Token、API Key、真实账号和客户数据。
- 报告桥只监听本机，只接受工作台白名单 Origin；路径分析限制在受控抖音输出目录，拒绝符号链接、路径穿越和异常 XLSX 压缩包。
- API Key 只在既有模型安全链路中使用，不进入 Python、报告桥、Excel、证据包、Markdown、DOM、日志或测试快照。
- 公开互动数据不等于成交、销售额、真实转化率或医学正确性；模型不得补写 Excel 外事实。
- 医药健康内容只做内容与运营层分析，不做诊断，不承诺疗效，不引导停换药，不使用绝对化表达或虚假权威背书。

## 当前风险

- `127.0.0.1:8768` 已解决原端口冲突；剩余风险是真实业务验收时需确认报告桥已按这一固定端口常驻启动。
- 抖音、小红书接口、页面结构、登录验证和风控会变化，采集能力不能按永久稳定接口管理。
- 模型结论质量仍需用真实业务样本人工复核；确定性校验能防止数字和证据漂移，但不能替代经营判断。
- 小红书不能直接复用抖音 Excel 字段和报告模板，必须单独设计和验收。
