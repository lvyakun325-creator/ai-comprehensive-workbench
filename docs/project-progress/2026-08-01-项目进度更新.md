# 2026-08-01 小红书正式站点真实采集验收

## 结论

竞品洞察 Agent 已完成一条小红书公开作品链接的真实端到端验收：正式 Sites 页面识别为小红书后，通过本机 `127.0.0.1:8766` 调用 `xiaohongshu-scraper`，最终生成 Markdown、JSON、爆款素材 Excel 和 14 张图片。此前页面提示“无法连接本地采集桥”的直接原因是正式站点 Origin 未被本机桥放行；修复后真实浏览器请求与真实抓取均成功。

本次只能证明“小红书单篇作品采集链路可用”，不能替代账号主页、抖音账号报告和长期稳定性验收。

## 已完成

- 小红书和抖音采集桥精确放行当前正式 Sites Origin。
- 支持浏览器访问本机服务所需的本地网络预检响应。
- 未列入白名单的网页 Origin 继续返回 403，不开放任意网页调用本机抓取服务。
- 在用户当前 Chrome 登录的正式站点复现并执行同一条公开小红书作品链接。
- 页面从“正在连接本地采集桥”进入完成态，未再出现连接失败。
- 生成 1 份 Markdown、1 份 JSON、1 份爆款素材 Excel 和 14 张作品图片。
- Excel ZIP/XLSX 结构完整性检查无错误。
- 报告桥 `127.0.0.1:8768` 已启动，正式 Origin 预检返回 204，非白名单 Origin 仍返回 403。
- 项目基线重新验证：Python 竞品运行时 92 / 92 通过；构建成功，Node / React 330 / 330 通过。
- 将静态四步说明升级为真实采集阶段：平台识别、本机 Skill 健康检查、平台抓取、结果保存。
- 本机服务连接失败与平台抓取失败分开提示，不再统一误报“服务未启动”。
- Sites V12 已发布，并用用户截图中的小红书链接验证页面从第 3/4 步推进至第 4/4 步。
- 项目基线更新为：构建成功，Node / React 332 / 332 通过。

## 进行中

- 小红书账号主页、Top 笔记详情和五维拆解报告的真实业务验收。
- 抖音真实账号 Excel 与证据型 Markdown 报告的真实业务验收。
- 本机抖音、小红书和报告桥的长期常驻与异常恢复机制。

## 尚未完成

- 小红书账号字段、笔记形态和互动口径专属的账号竞品报告。
- 正式 Sites 到本机 `127.0.0.1:8768` 报告桥的真实模型全链路验收。
- 多设备、浏览器重启和平台登录态失效后的回归验收。

## 验收证据

| 验证项 | 结果 |
|---|---|
| 正式站点 | Sites V12，仅所有者可访问 |
| 平台识别 | 小红书链接正确路由至 `xiaohongshu-scraper` |
| 本机采集桥 | `127.0.0.1:8766` 健康检查 200 |
| 生产 Origin 预检 | 204，精确回显允许 Origin，并允许本地网络请求 |
| 非白名单 Origin | 403 `origin_not_allowed` |
| 真实页面状态 | 显示抓取完成和本地输出目录 |
| 真实产物 | Markdown、JSON、爆款素材 Excel、14 张图片 |
| Excel 完整性 | 无压缩结构错误 |
| 报告桥 | `127.0.0.1:8768` 健康 200；正式 Origin 预检 204；非白名单 Origin 403 |
| 网页阶段进度 | 正式站点显示第 3/4 步“正在抓取平台数据”，完成后显示第 4/4 步“抓取结果已保存” |
| 项目测试 | Python 92 / 92 通过；`npm test` 构建成功，Node / React 332 / 332 通过 |

## 风险与边界

- 采集依赖小红书页面结构、公开可见范围和本机独立登录态，平台变化后仍可能需要维护。
- 普通 Chrome 登录态与采集 Skill 的独立浏览器登录态分开管理，不复制、不导出 Cookie。
- 本次只做公开内容的只读采集，没有点赞、收藏、关注、评论或发布。
- 作品涉及健康内容；抓取结果是竞品素材，不代表医学结论正确，后续分析仍须遵守不诊断、不承诺疗效、不引导停换药的边界。
