# 2026-08-03 抖音真实采集链路修复

## 结论

竞品洞察 Agent 已使用用户授权的抖音账号分享短链完成真实采集与证据整理验证。网页第 2 步的即时失败已排除；第 3 步证据整理接口已恢复。

## 已完成

- 停止小红书登录与采集验证，切换为抖音账号链路。
- 确认分享短链有效，并正确解析为账号主页。
- 定位抖音采集桥即时失败根因：Python HTTPS 未加载有效 CA 证书。
- 为托管的抖音采集桥注入 Skill 独立环境内的 `certifi` CA 路径并重启服务。
- 使用生产 Sites Origin 调用采集桥，返回 HTTP 200 和完整任务级合同。
- 真实抓取账号“医心医意海燕”186 条作品，生成结构化 JSON 与 Excel。
- 发现报告桥仍加载 8 月 1 日旧代码并重启；`/analyze-artifacts` 已返回 HTTP 200、`evidence_ready`。

## 验证证据

| 项目 | 结果 |
|---|---|
| 抖音采集健康检查 | `127.0.0.1:8765/health` 返回 ready |
| 真实采集请求 | HTTP 200 |
| 识别类型 | `account` / `douyin-account` |
| 账号昵称 | 医心医意海燕 |
| 作品数量 | 186 |
| 结构化数据 | 171,189 bytes |
| Excel | 28,041 bytes |
| 报告桥健康检查 | `127.0.0.1:8768/health` 返回 healthy |
| 证据整理 | HTTP 200，`evidence_ready` |

## 尚未完成

- 仍需在正式网页点击“抓取并分析”，验收浏览器模型生成、报告组装、成果包登记和自动切换成果页。
- 小红书账号主页真实采集暂时停止，不作为本次抖音验收范围。

## 风险与边界

- 抖音平台风控、登录态和页面结构仍可能随时间变化。
- 本次未读取、导出或记录 Cookie 内容，也未执行任何互动或发布操作。
- 健康类竞品数据仅用于内容表现分析，不代表医学正确性或商业转化结果。
