# 2026-08-03 抖音单视频下载/转写增强安装

## 已完成

- 保留原有抖音主页抓取、账号 Excel 和报告能力。
- 在 standalone 和实际 Codex Skill 环境中补齐 `yt-dlp`、`faster-whisper`、`opencc-python-reimplemented`。
- 将离线 `faster-whisper-small` 模型放到 standalone 根目录，实际 Skill 复用该本地模型。
- 完成 CPU `int8` 模型加载验证，可在不联网下载模型的情况下进入转写。
- 确认 `ffmpeg 7.1`、Chrome 和 Playwright 可用。
- 网页端提交抖音链接时自动开启 `--transcribe`；账号模式忽略，单作品模式执行本地转写。
- 真实公开作品 `7456736867308817702` 已通过生产 Origin 请求完成抓取、临时下载、本地转写和证据整理。

## 验收结果

| 项目 | 结果 |
|---|---|
| `yt-dlp` | 2026.07.04，可用 |
| `faster-whisper` | 1.2.1，可用 |
| `opencc` | 0.1.7，可用 |
| 离线模型 | 已发现并成功加载 |
| 安装版测试 | 22 / 22 通过 |
| standalone 测试 | 22 / 22 通过 |
| 主页抓取基础状态 | ready |
| 真实转写 | `transcribe_success:local_faster_whisper_small`，476 字 |
| 证据链 | `douyin-content` / `content` 批次已包含转写文稿和互动数据 |
| 项目回归 | Python 195 / 195；Node / React 389 / 389；构建和类型检查通过 |

## 尚未完成

- Sites 页面端到本机 Skill 的真实单作品链路已验收；仍需使用者在页面上执行一次最终业务体验确认。

## 风险与边界

- 抖音实时风控、视频地址时效和网络状态仍可能影响单次抓取。
- 本次未索要账号密码，未读取、导出、上传或记录 Cookie/登录态。
- 视频转写属于 ASR 结果，专业术语和同音词仍需大模型语义校对，不得据此补写不存在的事实。

## 线上状态

- Sites V15 已发布，保持仅所有者可访问；竞品洞察页面已显示抖音单视频本地转写说明。
