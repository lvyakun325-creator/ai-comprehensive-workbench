# 2026-08-01 竞品单链接成果包里程碑

## 结论

竞品洞察的本分支已完成单链接入口、抖音/小红书账号与单内容四路合同、证据型报告、成果包归档及完成后自动聚焦的本机自动化闭环。此结论仅代表本机回归：没有抓取真实账号，没有读取或导出 Cookie、登录态或凭据，也没有发布 Sites。

## 已完成

- 仅保留链接输入；Excel 上传入口已移除。
- 账号与单内容按平台严格路由；账号指标和单内容报告边界分开，单内容不冒充账号分析。
- 账号报告包含确定性 Top 10、起号期与指标；单内容仅使用其允许的证据与章节。
- 一个任务对应一个成果包，包含主报告、受控子文件、ZIP、预览、访达定位、分类和缺失状态。
- v1 历史任务无损迁移；任务完成后自动切换到成果文件并聚焦匹配成果包。
- 本地服务健康合同、运行时、前端与外部 Skill 离线合同均已执行。

## 进行中

- 用户授权后的真实账号/公开链接业务验收：核对平台返回完整性、四路路由、成果质量和报告的经营可用性。
- 小红书账号主页和抖音账号级报告的真实业务验收。

## 尚未完成

- 本分支正式发布到 Sites。此次未执行发布，现有 Sites 仍可能是旧版。
- 线上页面调用本机服务的真实业务验收。
- 竞品之外 Agent 的统一任务、成果和数据库能力。

## 测试基线

| 验证项 | 结果 |
|---|---|
| Python 竞品运行时 | 最终全量 185 / 185 通过 |
| 项目记录锁回归 | 项目记录模块 40 / 40 通过；两次全量重跑曾出现 `record_store_locked` 瞬态，未发现锁持有者，后续最终全量通过 |
| 外部抖音 Skill 离线合同 | 10 / 10 通过 |
| 外部小红书 Skill 离线合同 | 6 / 6 通过 |
| TypeScript | `npm run typecheck` 通过 |
| Lint | 0 error、5 条既有 warning |
| Node / React | `npm test` 384 / 384 通过 |
| 构建 | 独立 `npm run build` 通过 |
| 本地健康烟测 | 8765、8766、8768 均返回 200，且服务身份正确 |

## 风险与发布决策

- 健康检查只验证本地合同，不代表真实平台抓取成功。
- 真实抓取必须由用户重新授权公开链接与当前登录态；本里程碑没有访问这类数据。
- 公开互动数据只用于内容表现判断，不能推断成交、销量或真实转化，也不能替代医疗建议。
- 正式发布须由用户明确确认；发布成功后才可更新线上状态与生产版本说明。
