# AI 综合工作台项目进度总览

- 项目：AI 综合工作台
- 首次建档：2026-07-30
- 最近更新：2026-08-04
- 当前阶段：抖音、小红书、内容矩阵三项验收通过，主线收口完成并与发布基线对齐
- 原始目标完成度：约 55%
- 前端与交互完成度：约 88%
- 发布状态：Sites V16 已正式发布且保持仅所有者可访问；当前稳定分支为 `main`

## 项目初衷

建设一个统一的 AI 经营工作台，用一个总控台管理模型、任务、成果和数据，并由 9 个专业 Agent 分工完成内容矩阵、竞品洞察、选题、标题、图文、写作、口播、获客视频和数据复盘。

最终目标不是做一个只有界面的聊天工具，而是形成可连续执行、可追踪进度、可沉淀成果、能提高内容产能与经营决策效率的业务系统。

## 当前结论

当前已经具备较完整的产品界面、模型配置、普通连续聊天、内容矩阵真实执行能力，以及竞品洞察 Agent 的本地双平台采集入口。

本次竞品洞察升级已完成单链接入口的本地闭环：仅接受抖音/小红书账号或单内容链接，确定性程序按四类合同生成证据、Top 10/起号期指标（仅账号适用）和受控成果；模型按 `strategy / performance / execution` 三批生成结构化判断；最终经过证据、数字、结构和医药健康合规校验后保存 Markdown。单内容不再冒充账号报告，成果会封装为一任务一成果包并可在任务完成后自动聚焦。官方模型走同源服务端代理，已审核的 APINebula 精确域名可走浏览器直连。

2026-08-04：内容矩阵、抖音 Skill、和小红书 Skill 三项业务验收已补齐并通过（内容矩阵 389 / 389、抖音 22 / 22、小红书 11 / 11）。`fix(content-matrix): align competitor model config assertions`（PR #10）与主线收口文案同步（PR #11）已完成合并，当前主线基线 `a7fc9f8` 与 `origin/main` 已一致。

该界面已以 Sites V11 发布到既有生产地址，源码对应 GitHub `main` 提交 `fcde7c6`。发布前将 Next.js 升级到 `16.2.12`，并固定已修复的 PostCSS `8.5.25`、Sharp `0.35.3`；生产依赖审计为 0 个已知漏洞。

2026-08-01 旧版曾用一条用户授权的小红书公开作品链接完成正式 Sites 页面到本机 `127.0.0.1:8766` 的真实采集：自动路由、独立登录态调用、Markdown、JSON、Excel 和 14 张图片落盘均成功。2026-08-03 已使用用户授权的抖音账号分享短链完成当前分支真实验收：采集桥识别为账号主页，抓取“医心医意海燕”186 条作品，输出任务级 JSON 与 Excel；报告桥随后返回 `evidence_ready`。全过程未导出、展示或记录 Cookie、登录态和凭据。小红书账号主页仍未完成真实业务验收。报告桥使用空闲固定端口 `127.0.0.1:8768`，不触碰仍占用 `8767` 的无关 `NewMediaCardMaker/0.4`。

同日将采集页原有的静态四步说明升级为真实阶段状态：平台识别完成后先检查本机 Skill 健康，再显示连接、平台抓取和结果保存进度；本地连接失败与平台抓取失败分别提示。Sites V12 已使用用户截图中的小红书链接完成正式网页实测，页面从第 3/4 步推进至第 4/4 步并显示本地输出目录。

Sites V14 已正式发布当前稳定版竞品任务与成果链路：提交抓取后先创建任务，记录平台、Skill 和脱敏后的项目链接；成功后登记 Excel、Markdown、JSON、图片目录和输出目录，自动切换到「成果文件」并聚焦本次任务。记录使用本地 JSON 原子写入，刷新页面或重新打开浏览器后仍可读取；成果可复制路径或在访达中显示。

Sites V15 在不改页面请求/响应合同的前提下接入抖音单视频本地转写：页面提交抖音作品链接后，采集桥自动为 Skill 开启 `--transcribe`；账号模式忽略该开关，单作品模式临时下载并使用本地 `faster-whisper-small` 转写。转写文稿进入结构化数据、Excel、content 证据批次和成果包；网页默认不持久保存原视频。

Sites V16 将竞品洞察模型从全局模型注册表中彻底分离：竞品配置页可单独选择 OpenAI 或 APINebula，填写 API 地址、模型名称和 API Key，并在连接测试成功后应用。竞品报告运行器只订阅这套会话级独立配置，不读取、不选择、也不回退到全局模型；即使全局模型已连接，竞品独立模型为空时仍停在证据就绪阶段。API Key 只保留在当前页面内存，刷新即清空；模型或凭据变化会中止旧请求并保留证据包。

2026-08-03 已将“基于抖音账号 Excel 生成 Markdown 分析报告”正式整合进安装版 `douyin-scraper`，并同步到原 standalone 安装包。默认抓取仍只生成 Excel；只有明确的账号分析意图或 `--report` 才继续生成 Markdown。已有账号 Excel 可直接调用 `report_generator.py`，不重新抓取、不调用 Cookie。网页竞品洞察继续使用现有受控大模型报告链路，采集桥默认合同未改，避免重复生成两套报告。

同日排除抖音网页采集的即时失败：根因是托管采集桥未继承 Python 所需的可信 CA 证书路径，短链解析在 HTTPS 握手阶段失败。为抖音采集桥注入 Skill 独立环境内的 `certifi` 证书后，同一分享文本从生产 Origin 调用 `127.0.0.1:8765/scrape` 返回 HTTP 200。另发现报告桥仍加载 8 月 1 日旧代码，重启后 `/analyze-artifacts` 从 404 恢复为 `evidence_ready`。

网页再次失败后进一步定位到任务记录迁移：schema v1 的已失败任务在迁移到 v2 时没有补齐 `inputKind`、`category`、`bundleId`，导致任务服务返回 `503 RECORD_STORE_DAMAGED`。已按 TDD 修复迁移逻辑，真实任务库从 v1 原子升级到 v2；18 条历史任务、5 个成果记录和 1 个旧成果包全部保留，迁移前文件已单独备份，生产 Origin 的任务查询恢复 HTTP 200。

同时复现了手机分享文案元数据紧贴短链的情况：原桥会把 `5@0.com:7pm` 误当成短链路径，抖音仅跳转首页。已让 `v.douyin.com` 链接只取首个分享码段，并同步安装版与 standalone；使用截图原文完成真实回归，返回账号“医心医意海燕”、186 条作品和 HTTP 200。

## 2026-08-03 抖音真实链路验证

| 验证项 | 结果 |
|---|---|
| 抖音短链解析 | 同一分享文本正确解析为账号主页 |
| 抖音采集桥 `127.0.0.1:8765` | 生产 Origin 请求返回 HTTP 200，12 字段任务合同完整 |
| 真实账号数据 | 账号“医心医意海燕”，抓取 186 条作品 |
| 任务级成果 | `结构化数据.json` 171,189 bytes；Excel 28,041 bytes |
| 报告桥 `127.0.0.1:8768` | 重启加载当前代码；`/analyze-artifacts` 返回 HTTP 200、`evidence_ready` |
| 任务记录服务 | v1→v2 迁移完成；18 条任务、5 个成果记录、1 个旧成果包保留；查询返回 HTTP 200 |
| 迁移回归 | 新增失败任务迁移测试；Python 竞品运行时 195 / 195 通过 |
| 紧贴分享尾巴 | 截图原文真实调用返回 HTTP 200；账号“医心医意海燕”，186 条作品 |
| 当前边界 | 抓取与证据整理已通过；浏览器模型生成、报告组装和成果包自动切换仍需页面最终验收 |
| 隐私边界 | 未读取、导出或记录 Cookie 内容；未执行点赞、关注、评论、收藏或发布 |

## 已完成

- [x] 工作台整体界面和导航框架
- [x] 9 个 Agent 的独立项目入口和界面隔离
- [x] 全局文案模型、图片模型配置界面
- [x] 模型连接测试、默认模型和 Agent 模型选择
- [x] 普通聊天、多轮连续对话、会话历史、停止和失败重试
- [x] 内容矩阵 Agent 的真实五阶段生成流程
- [x] 竞品洞察 Agent 独立采集控制台
- [x] 抖音账号抓取 Skill 安装、基础依赖隔离和 macOS Chrome 兼容
- [x] 小红书账号抓取 Skill 安装、独立 Python 环境和 Codex 路径适配
- [x] 抖音/小红书链接自动识别与跨平台 Skill 路由
- [x] 仅监听本机的抖音、小红书采集桥与写端点 Origin 限制
- [x] 竞品洞察只保留单链接入口，移除 Excel 上传入口
- [x] 抖音 Excel 字段识别、数值清洗、稳定排名、起号期筛选和数据完整性记录
- [x] 确定性证据包、稳定证据编号和受控本地证据保存
- [x] `strategy / performance / execution` 三批模型生成、失败批次重试和迟到响应丢弃
- [x] 官方模型同源代理与受控浏览器直连路由
- [x] 章节 Schema、证据引用、数字、固定结构和医疗健康合规校验
- [x] Markdown 报告本地保存、预览、下载和路径复制
- [x] 原始 Excel 不改写，上传临时副本分析后删除
- [x] 报告、证据和临时输出目录逐级 no-follow 打开，基于目录 fd 独占/原子写入
- [x] 严格抖音账号工作簿合同、strategy 账号上下文与单批一次安全自动重试
- [x] 报告桥迁移到 `127.0.0.1:8768`，Python/TypeScript 端口一致性回归通过
- [x] Agent 任务列表、状态、进度和筛选界面
- [x] Markdown 成果预览、复制和下载界面
- [x] 竞品洞察证据型报告界面已发布至既有 Sites 生产地址（V11）
- [x] 生产依赖安全补丁：Next.js `16.2.12`、PostCSS `8.5.25`、Sharp `0.35.3`
- [x] 正式 Sites 精确 Origin 已接入本机抖音/小红书采集桥，并支持浏览器本地网络预检
- [x] 小红书单篇公开作品真实采集验收：Markdown、JSON、爆款素材 Excel 和 14 张图片完整落盘
- [x] 报告桥 `127.0.0.1:8768` 已启动，并精确放行正式 Sites Origin 与本地网络预检
- [x] 竞品抓取四阶段真实进度、服务健康预检和分层错误提示已发布至 Sites V12
- [x] 竞品抓取任务按平台、Skill 和脱敏链接持久记录
- [x] Excel、Markdown、JSON、图片目录和输出目录自动登记
- [x] 完整流程成功后自动切换「成果文件」并聚焦当前任务
- [x] 成果路径复制、访达定位和文件丢失状态提示
- [x] 竞品任务/成果本地 JSON 原子持久化已发布至 Sites V13
- [x] 抖音/小红书账号与单内容四路路由、证据合同和确定性指标
- [x] 单内容报告与账号报告的严格边界，避免以单内容冒充账号判断
- [x] 一任务一成果包、主报告预览、ZIP 下载、访达定位与 v1 历史记录无损迁移
- [x] 成果包按平台/对象分类，子文件默认折叠，完成后自动跳转并聚焦对应成果包
- [x] 本次单链接成果包自动化回归、三路本地健康合同烟测和安全边界检查
- [x] 抖音账号 Excel 独立 Markdown 报告生成器、字段别名与 `w/万/亿` 数值解析
- [x] 抖音账号 `--report` 显式路由；默认抓取与单视频流程保持原行为
- [x] Top 10、起号期 Top 5、内容方向、证据型对标建议、7 天清单与医疗健康合规提醒
- [x] 安装版 Skill 与 standalone 安装包同步，官方 Skill 结构校验通过
- [x] 竞品洞察独立模型配置页面、连接测试和会话级凭据隔离
- [x] 竞品报告运行器移除全局模型读取与回退路径
- [x] 竞品独立模型变更时中止旧请求、保留证据包并支持安全重试
- [x] 竞品洞察独立模型配置正式发布至 Sites V16
- [x] 内容矩阵、抖音 Skill、小红书 Skill 三项验收基线同步为通过，PR #10/#11 已合并，主线收口基线锁定为 `a7fc9f8`

## 进行中

- [ ] 竞品洞察抖音真实账号首次业务验收
  - 需要用户在本机完成平台验证，并用用户授权的公开链接核对采集完整性、四路路由和报告业务价值。
  - 报告桥已使用 `127.0.0.1:8768`；本任务未抓取真实账号、未读取 Cookie 或登录态。
- [ ] 竞品洞察小红书账号主页首次采集验收
  - 单篇公开作品已通过正式站点真实采集；账号主页、Top 笔记和五维拆解报告仍待真实业务验收。
- [ ] 模型配置稳定性加固
  - 当前可用，仍需继续兼容不同供应商的 CORS、超时和安全错误返回。
- [ ] 内容矩阵正式站点五阶段实网生成复测
  - API Key 按设计只存在当前页面内存，刷新即清空；正式站点实网生成需由使用者在当前会话重新填写。

## 尚未完成

- [ ] 小红书字段与内容形态专属的竞品分析报告
- [ ] 正式线上站点调用本机抖音账号与报告桥的真实业务验收
- [ ] 聊天、证据包和其他 Agent 任务/成果的统一数据库持久化（竞品任务/成果已实现本机 JSON 长期保留）
- [ ] 除内容矩阵与竞品洞察外，其余 7 个 Agent 的真实执行能力
- [ ] 多 Agent 串联协同和任务交接
- [ ] 后台任务队列、排队、失败重试和定时执行
- [ ] 天猫、京东、拼多多、视频号、企微等外部平台数据接入
- [ ] 基于真实经营数据的数据概览和复盘
- [ ] 真实资产库、文件存储、分类、搜索和分享
- [ ] 实际图片生成工作流
- [ ] 聊天附件、工具调用和语音能力
- [ ] 用户登录、角色权限、操作审计和多人协作
- [ ] 可用的系统设置模块
- [ ] 服务端密钥安全存储
- [ ] 模型用量、Token 成本和预算预警

## 当前关键指标

| 指标 | 当前值 |
|---|---:|
| 真实可执行专业 Agent | 2 / 9（竞品洞察小红书单篇真实采集已验收，账号级报告待验收） |
| 已打通真实核心能力 | 普通聊天、内容矩阵、双平台本地采集入口、小红书单篇线上真实采集、抖音证据型报告本地闭环 |
| 前端与交互完成度 | 约 88% |
| 原始业务目标完成度 | 约 55% |
| 抖音 Skill 账号/单作品测试 | 安装版 22 / 22；standalone 22 / 22 通过 |
| 项目 Python 竞品运行时测试 | 195 / 195 通过 |
| 本次项目 Python 竞品运行时测试 | 195 / 195 通过 |
| 本次项目 Node / React 测试 | 389 / 389 通过（含构建） |
| 本次外部 Skill 离线契约测试 | 抖音 22 / 22；小红书 11 / 11 通过 |
| 数据持久化 | 竞品任务/成果已本机长期保留；其他数据尚未统一持久化 |
| 本次线上版本 | Sites V16 已部署成功，保持仅所有者可访问；正式地址不变 |

## 2026-08-04 主线收口与正式发布基线

| 项目 | 状态 |
|---|---|
| 主线收口版本 | `main` = `a7fc9f8`（PR #11，含 PR #10 收口） |
| 分支对齐 | `origin/main` 快照已对齐到本地 `main` |
| 关键测试基线 | Node / React 389 / 389；抖音 22 / 22；小红书 11 / 11；Python 竞品运行时 195 / 195 |
| 发布动作 | Sites V16 维持在既有发布地址不变，仅所有者可见 |
| 收口状态 | 代码主线与正式发布基线口径已锁定，未新增代码 |

## 2026-08-03 竞品洞察独立模型配置

| 验证项 | 结果 |
|---|---|
| 配置入口 | 「竞品洞察 Agent → Agent 配置」提供独立服务商、API 地址、模型名称和 API Key |
| 支持路径 | OpenAI 官方地址走同源服务端代理；APINebula 精确域名走浏览器直连 |
| 全局隔离 | 竞品报告运行器不再导入或调用全局模型注册表；全局模型已连接也不会被竞品使用 |
| 凭据边界 | API Key 只保留在当前页面内存，不写入本地存储、项目文件或日志，刷新即清空 |
| 运行保护 | 配置或凭据变化会中止旧模型请求；证据包与已通过批次按既有规则保留 |
| 自动化 | `npm test` 构建通过，Node / React 366 / 366；TypeScript 通过；Lint 0 error、5 条既有 warning |
| GitNexus | 中心运行链路风险为 CRITICAL；三批生成、停止、失败重试、迟到响应和成果封装均已回归 |
| 正式发布 | Sites V16 部署成功，源码提交 `6bfa5c8`，正式地址不变，仅当前所有者可访问 |

## 2026-08-03 抖音单视频下载/转写增强安装

| 验证项 | 结果 |
|---|---|
| 增强依赖 | 实际 Codex Skill 独立环境已安装 `yt-dlp 2026.07.04`、`faster-whisper 1.2.1`、`opencc-python-reimplemented 0.1.7` |
| 离线模型 | `faster-whisper-small` 已放入 standalone 根目录，权重 SHA-256 与原包一致；实际 Skill 通过本地链接复用 |
| 模型加载 | CPU `int8` 实际加载成功，转写时不需联网下载 Whisper small |
| 系统工具 | `ffmpeg 7.1` 可用，Chrome 和 Playwright 基础环境可用 |
| 单视频能力 | 支持视频地址解析、`--transcribe` 转写和 `--save-video` 保存视频 |
| 真实单作品验收 | 抓取公开作品 `7456736867308817702`，33 秒完成本地转写，文稿 476 字，进入 `douyin-content` 证据批次 |
| 回归验证 | 安装版 22 / 22、standalone 22 / 22、项目运行时 195 / 195、Node / React 389 / 389 通过 |
| 界面与隐私 | 已增加单视频本地转写说明并发布 Sites V15；未读取、导出或上传 Cookie/登录态 |

## 2026-08-03 抖音账号报告 Skill 升级

| 验证项 | 结果 |
|---|---|
| 默认抓取 | 不带 `--report` 时只生成 Excel，不生成 Markdown |
| 抓取并分析 | 账号模式带 `--report` 时先生成 Excel，再生成 Markdown，并返回 `excel_path` 与 `markdown_path` |
| 已有 Excel | `python report_generator.py "<账号数据.xlsx>"` 可独立生成 Markdown，不触发抓取或 Cookie |
| 单视频边界 | 单视频抓取、保存和转写入口未改；即使出现 `--report` 也不生成账号报告 |
| 数据与证据 | 综合互动量使用点赞、评论、收藏、分享的可用字段；缺字段、缺时间、空作品和小样本均明确披露 |
| 精度 | 新抓取报告优先使用原始结构化数字；已有 Excel 兼容 `w`、`万`、`亿` 展示值 |
| 医疗健康边界 | 报告不做疾病诊断、不承诺疗效、不引导停换药或调整剂量 |
| 自动化 | 安装版 20 / 20、standalone 20 / 20；`main.py`、`report_generator.py` 语法检查通过；两份 Skill 官方校验通过 |
| 隐私与真实验收 | 未执行真实抖音抓取，未读取或导出 Cookie；当前使用离线账号工作簿验证，真实账号 Excel 仍待业务验收 |
| 线上发布 | 未发布 Sites；网页采集桥和现有受控模型报告链路未改 |

## 2026-08-01 竞品单链接成果包里程碑

| 验证项 | 结果 |
|---|---|
| 单入口与四路合同 | 抖音/小红书账号、单内容链接均有严格合同；无 Excel 上传入口 |
| 成果包闭环 | 一任务一成果包，主报告、ZIP、访达定位、分类、自动跳转与历史迁移均纳入自动化验证 |
| Python 运行时 | 全量 194 / 194 连续两轮通过；其中项目记录 45 / 45、源读取 15 / 15、服务 37 / 37、桥接 22 / 22 通过 |
| 外部 Skill | 抖音 10 / 10、小红书 10 / 10 离线合同通过；小红书持久化产物已覆盖请求参数、嵌入日志文本与凭据哨兵脱敏 |
| 前端 | typecheck 通过；lint 0 error、5 条既有 warning；`npm test` 389 / 389 且构建通过；客户端 41 / 41、UI 128 / 128 通过 |
| 健康身份合同 | 抓取、报告和记录客户端均在业务请求前严格核验服务身份，错误身份关闭失败；本轮未启动真实服务 live smoke |
| 安全与耐久性 | 请求 token 不进入证据、报告、项目 ZIP 或外部 Skill 持久化产物；锁冲突稳定映射 503，归档快照不可变且与实时文件独立限额 |
| 真实业务与发布 | 本分支未执行任何真实平台抓取（账号或单内容），未读取/导出 Cookie、登录态或凭据；旧版单笔记证据不构成本分支四路验收；未发布 Sites，现有 Sites 仍可能旧版 |

## 2026-08-01 任务成果长期保留验证

| 验证项 | 结果 |
|---|---|
| Sites 发布 | V13 发布成功，保持仅所有者可访问 |
| 任务长期保留 | 本地 `project-records.json` 原子写入，页面刷新后重新读取 |
| 成果范围 | Excel、Markdown、JSON、图片目录、输出目录 |
| 完成后行为 | 自动切换「成果文件」，仅展示当前任务成果 |
| 本地桥接 | 健康检查 200，任务读取 200，生产预检 204，非白名单 Origin 403 |
| 测试基线 | Node / React 344 / 344；Python 106 / 106；Lint 和 TypeScript 通过 |
| 依赖安全 | `npm audit --omit=dev`：0 个已知漏洞 |

## 2026-08-01 小红书真实链路验证

| 验证项 | 结果 |
|---|---|
| 正式站点到 `127.0.0.1:8766` | 同一条公开作品链接真实请求成功，页面显示抓取完成 |
| 生产 Origin 预检 | 精确生产域名返回 204，并返回本地网络许可；未列入白名单的 Origin 返回 403 |
| 小红书抓取产物 | Markdown、JSON、爆款素材 Excel、14 张图片 |
| Excel 完整性 | ZIP/XLSX 结构检测无错误 |
| 报告桥 `127.0.0.1:8768` | 健康检查 200；正式 Origin 预检 204；非白名单 Origin 403 |
| Python 竞品运行时 | 92 / 92 通过 |
| 项目基线 | `npm test` 构建成功，332 / 332 通过 |
| 网页阶段进度 | 第 1 步平台识别、第 2 步连接本地 Skill、第 3 步平台抓取、第 4 步保存结果均绑定真实状态 |
| 隐私边界 | 未导出、展示或记录 Cookie；未执行点赞、收藏、关注、评论或发布 |

## 2026-07-31 证据型报告最新验证

| 验证项 | 结果 |
|---|---|
| Python 竞品运行时全量 | 91 / 91 通过 |
| `npm test` | 构建通过，Node / React 330 / 330 通过 |
| TypeScript 类型检查 | 通过 |
| Lint | 通过，0 error / 0 warning |
| 生产依赖审计 | `npm audit --omit=dev`：0 个已知漏洞 |
| 脱敏端到端夹具 | 上传和受控路径均生成真实证据包；三批 mock 模型结果全部校验并组装 Markdown |
| 原始脱敏 Excel | 前后 SHA-256 一致，未改写 |
| 报告固定内容 | Top 10、起号期 Top 5、5 个选题方向、3 个拍法模板、7 天清单均通过回读 |
| 抖音 Skill 离线契约 | `/Users/lvyakun/.codex/skills/douyin-scraper`，6 / 6 通过 |
| 抖音采集桥 `127.0.0.1:8765` | 身份为 `DouyinScraperBridge/1.0`，健康检查 200；恶意 Origin 写请求 403 |
| 小红书采集桥 `127.0.0.1:8766` | 身份为 `XiaohongshuScraperBridge/1.0`，健康检查 200；恶意 Origin 写请求 403 |
| 报告桥 `127.0.0.1:8768` | 健康响应标识 `competitor-insight-report` 且返回 200，恶意 Origin 写请求 403 |
| 无关服务 `127.0.0.1:8767` | `NewMediaCardMaker/0.4` 保持原状，未终止、重启或改动 |
| 真实抖音/小红书抓取 | 未执行；未读取 Cookie、登录态或真实账号数据 |
| 真实模型调用 | 未执行；未使用或记录 API Key |
| 部署状态 | Sites V11 已成功发布，生产源码 `fcde7c6`，保持仅所有者可访问 |

## 本地输出与安全边界

- 报告目录：`outputs/competitor-insight/reports/`
- 证据目录：`outputs/competitor-insight/reports/evidence/`
- 抖音抓取输出：`outputs/competitor-insight/douyin/`
- 小红书抓取输出：`outputs/competitor-insight/xiaohongshu/`
- 竞品任务/成果记录：`outputs/competitor-insight/.workbench/project-records.json`
- 项目链接持久化前会移除 token、source、签名和追踪参数；仅保留安全业务参数。
- 成果登记与访达定位只接受 `outputs/competitor-insight/douyin`、`xiaohongshu` 和 `reports` 三个受控根目录，拒绝符号链接和路径越界。
- 报告桥只允许受控抖音输出目录中的普通 `.xlsx`，上传上限为 50 MB，并校验 ZIP/XLSX 签名、解压预算、路径穿越和符号链接；所有持久/临时输出基于验证后的目录 fd 创建。
- API Key 不进入 Python、报告桥、Excel、证据包、Markdown、DOM、日志或测试快照。
- 公开互动数据只能用于内容表现判断，不能推断成交量、销售额、真实转化率或医学正确性。
- 医药健康建议不得形成诊断、疗效承诺、停换药建议、绝对化表达或虚假权威背书。

## 进度同步规则

以下情况发生后，必须同步更新本文件和 Obsidian 知识库镜像：

1. 一个模块由“未完成”变为“进行中”或“已完成”。
2. 新增或完成一个真实 Agent。
3. 发生正式部署、回滚或线上验证。
4. 自动化测试数量或通过状态发生变化。
5. 出现影响交付的阻塞、风险或范围调整。
6. 项目初衷、优先级或业务目标发生变化。

同步时必须标注日期，并记录本次完成、进行中、风险阻塞、关键指标和对原始目标完成度的最新判断。达到里程碑或状态明显变化时，在本目录新增日期快照。

## 历史快照

- [2026-08-04 主线收口与正式发布基线](./2026-08-04-主线收口与正式发布基线.md)
- [2026-08-03 抖音单视频下载转写增强安装](./2026-08-03-抖音单视频下载转写增强安装.md)
- [2026-08-03 抖音账号报告 Skill 升级里程碑](./2026-08-03-抖音账号报告Skill升级里程碑.md)
- [2026-08-01 竞品单链接成果包里程碑](./2026-08-01-竞品单链接成果包里程碑.md)
- [2026-08-01 竞品任务成果持久化里程碑](./2026-08-01-竞品任务成果持久化里程碑.md)
- [2026-08-01 小红书正式站点真实采集验收](./2026-08-01-项目进度更新.md)
- [2026-07-31 竞品洞察 Agent 证据型报告里程碑](./2026-07-31-项目进度更新.md)
- [2026-07-30 项目进度盘点](./2026-07-30-项目进度盘点.md)
- [2026-07-30 模型修复与上线里程碑](./2026-07-30-模型修复与上线里程碑.md)
